# Extract testing dockerfile

This is a test for a script that extracts the contents of a Dockerfile up to
a given stage.

## Running

```
rm -rf .testing.Dockerfile && ./extract-testing-dockerfile
```
